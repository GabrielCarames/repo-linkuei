import Colectivo.*
import Recaudacion.*
import Combustible.*
import Pasajeros.*
import Paradas.*

test "Combustible del colectivo" {
	assert.throwsExceptionLike(	
   		new UserException("a"),
   		{ (combustible.actual() <= 0) }
	)
}